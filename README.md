# uros-project/README.md

```
# uros-project

这是一个多模块的 Spring Boot 项目，包含多个模块以支持不同的功能。

## 模块

### uros-kernel-base

该模块是应用程序的核心，包含主要的业务逻辑和配置。

#### 功能

- 启动 Spring Boot 应用
- 提供基础的服务和功能

## 使用方法

1. 克隆项目到本地
2. 使用 Maven 构建项目
3. 运行 `UrosKernelApplication` 启动应用程序
```